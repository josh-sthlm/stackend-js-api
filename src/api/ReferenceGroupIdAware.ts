export interface ReferenceGroupIdAware {
  /**
   * An id used to differentiate a set of objects from another set of objects in the same context.
   */
  referenceGroupId: number;
}
export default ReferenceGroupIdAware;
